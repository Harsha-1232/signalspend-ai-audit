# Tests

Run all tests:

```bash
npm test
```

## Automated Tests

- `tests/audit.test.ts` — efficient small Copilot setup does not get fake savings.
- `tests/audit.test.ts` — spend above listed public pricing is normalized.
- `tests/audit.test.ts` — high unavoidable API spend triggers discounted-credit savings.
- `tests/audit.test.ts` — tiny teams on richer plans get same-vendor downgrade recommendations.
- `tests/audit.test.ts` — savings aggregate correctly across multiple tools.
- `tests/audit.test.ts` — optimized stacks get an honest fallback summary.
